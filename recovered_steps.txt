
==================================================
STEP: 74
DESC: "Add Web parsing/highlighting helper and update Web Admin Chat timeline style to match royal blue/charcoal bubble colors and unified attachment cards layout."
==================================================
Failed to decode chunks: Invalid control character at: line 1 column 2049 (char 2048)
RAW CHUNKS: [{"AllowMultiple":false,"EndLine":2046,"ReplacementContent":"      const getAttachmentUrl = (url, type) => {\n        if (!url) return '';\n        if (url.includes('drive.google.com/uc')) {\n          const match = url.match(/[?&]id=([^&]+)/);\n          if (match && match[1]) {\n            if (type === 'image') {\n              return `https://lh3.googleusercontent.com/d/${match[1]}`;\n            } else {\n              return `https://drive.google.com/uc?export=download&id=${match[1]}`;\n            }\n          }\n        }\n        return url;\n      };\n\n      const renderParsedTextWeb = (text) => {\n        if (!text) return null;\n        const regex = /(\"[^\"]+\")|(--\\w+(?:\\s+\\d+:\\d+|\\s+\\S+)?)/g;\n        const parts = text.split(regex);\n        return parts.map((part, index) => {\n          if (!part) return null;\n          if (part.startsWith('\"') && part.endsWith('\"')) {\n            return <span key={index} className=\"text-brand-cyan font-bold\">{part}</span>;\n          }\n          if (part.startsWith('--')) {\n            return <span key={index} className=\"text-brand-violet font-mono font-bold\">{part}</span>;\n          }\n          return <span key={index}>{part}</span>;\n        });\n      };","StartLine":2030,"TargetContent":"      const getAttachmentUrl = (url, type) => {\n        if (!url) return '';\n        if (url.includes('drive.google.com/uc')) {\n          const match = url.match(/[?&]id=([^&]+)/);\n          if (match && match[1]) {\n            if (type === 'image') {\n              return `https://lh3.googleusercontent.com/d/${match[1]}`;\n            } else {\n              return `https://drive.google.com/uc?export=download&id=${match[1]}`;\n            }\n          }\n        }\n        return url;\n      };"},{"AllowMultiple":false,"EndLine":5236,"ReplacementContent":"                                      return (\n                                        <div \n                                          key={msg.id || index} \n                                    
<truncated 23026 bytes>

==================================================
STEP: 187
DESC: "Add state variables and refs for voice recording on web in index.html."
==================================================
TARGET:
"      const [replyInput, setReplyInput] = useState('');\n      const [sendingReply, setSendingReply] = useState(false);\n      const replyInputRef = useRef(null);\n\n      useEffect(() => {"
REPLACEMENT:
"      const [replyInput, setReplyInput] = useState('');\n      const [sendingReply, setSendingReply] = useState(false);\n      const replyInputRef = useRef(null);\n\n      // Voice Recording States on Web\n      const [isAdminRecording, setIsAdminRecording] = useState(false);\n      const [adminRecordingDuration, setAdminRecordingDuration] = useState(0);\n      const adminMediaRecorderRef = useRef(null);\n      const adminAudioChunksRef = useRef([]);\n      const adminRecordingTimerRef = useRef(null);\n\n      useEffect(() => {"

==================================================
STEP: 191
DESC: "Update handleAdminFileUpload with correct async error handling and add startAdminRecording, stopAdminRecording, and cancelAdminRecording helper functions on web."
==================================================
TARGET:
"      const handleAdminFileUpload = async (e) => {\n        const file = e.target.files[0];\n        if (!file || !selectedChatId) return;\n\n        setSendingReply(true);\n\n        try {\n          const reader = new FileReader();\n          reader.onload = async () => {\n            const base64Data = reader.result.split(',')[1];\n            \n            let type = 'document';\n            if (file.type.startsWith('image/')) type = 'image';\n            else if (file.type.startsWith('video/')) type = 'video';\n            else if (file.type.startsWith('audio/')) type = 'audio';\n\n            const payload = {\n              action: 'upload_file',\n              fileName: file.name,\n              mimeType: file.type,\n              base64Data: base64Data\n            };\n\n            const response = await fetch(GOOGLE_SHEET_WEBHOOK_URL, {\n              method: 'POST',\n              headers: { 'Content-Type': 'text/plain;charset=utf-8' },\n              body: JSON.stringify(payload),\n            });\n            const result = await response.json();\n\n            if (result && result.status === 'success' && result.fileUrl) {\n              const msgData = {\n                sender: 'mentor',\n                name: 'Anurag KM (Mentor)',\n                fileUrl: result.fileUrl,\n                fileName: file.name,\n                type: type,\n                timestamp: isFirebaseMock ? new Date() : firebase.firestore.FieldValue.serverTimestamp()\n              };\n\n              if (isFirebaseMock) {\n                const updatedMessages = { ...mockMessages };\n                if (!updatedMessages[selectedChatId]) updatedMessages[selectedChatId] = [];\n                updatedMessages[selectedChatId].push({ id: `mentor_${Date.now()}`, ...msgData });\n                setMockMessages(updatedMessages);\n                setChatMessages(updatedMessages[selectedChatId]);\n              } else {\n                await firebaseDb.collection('chats').doc(selectedChatId).collection('messages').add(msgData);\
<truncated 708 bytes>
REPLACEMENT:
"      const handleAdminFileUpload = async (e) => {\n        const file = e.target.files[0];\n        if (!file || !selectedChatId) return;\n\n        setSendingReply(true);\n\n        try {\n          const reader = new FileReader();\n          reader.onload = async () => {\n            try {\n              const base64Data = reader.result.split(',')[1];\n              \n              let type = 'document';\n              if (file.type.startsWith('image/')) type = 'image';\n              else if (file.type.startsWith('video/')) type = 'video';\n              else if (file.type.startsWith('audio/')) type = 'audio';\n\n              const payload = {\n                action: 'upload_file',\n                fileName: file.name,\n                mimeType: file.type,\n                base64Data: base64Data\n              };\n\n              const response = await fetch(GOOGLE_SHEET_WEBHOOK_URL, {\n                method: 'POST',\n                headers: { 'Content-Type': 'text/plain;charset=utf-8' },\n                body: JSON.stringify(payload),\n              });\n              const result = await response.json();\n\n              if (result && result.status === 'success' && result.fileUrl) {\n                const msgData = {\n                  sender: 'mentor',\n                  name: 'Anurag KM (Mentor)',\n                  fileUrl: result.fileUrl,\n                  fileName: file.name,\n                  type: type,\n                  timestamp: isFirebaseMock ? new Date() : firebase.firestore.FieldValue.serverTimestamp()\n                };\n\n                if (isFirebaseMock) {\n                  const updatedMessages = { ...mockMessages };\n                  if (!updatedMessages[selectedChatId]) updatedMessages[selectedChatId] = [];\n                  updatedMessages[selectedChatId].push({ id: `mentor_${Date.now()}`, ...msgData });\n                  setMockMessages(updatedMessages);\n                  setChatMessages(updatedMessages[selectedChatId]);\n                } else {\n                  await
<truncated 6571 bytes>

==================================================
STEP: 195
DESC: "Update index.html admin chat input container to support web MediaRecorder audio recording, show a visual recording indicator, allow canceling, and use the correct paperclip SVG path."
==================================================
TARGET:
"                              <form onSubmit={handleSendReply} className=\"p-4 border-t border-white/5 bg-zinc-950/10 flex gap-3 items-center\">\n                                <div className=\"flex-grow flex items-center bg-white/[0.03] border border-white/10 rounded-2xl px-3 py-1.5 focus-within:border-brand-cyan/40 transition duration-300\">\n                                  {/* Hidden File Input */}\n                                  <input \n                                    type=\"file\" \n                                    id=\"admin-file-input\" \n                                    className=\"hidden\" \n                                    onChange={handleAdminFileUpload} \n                                    disabled={sendingReply}\n                                  />\n                                  \n                                  {/* Attachment Button */}\n                                  <button\n                                    type=\"button\"\n                                    onClick={() => document.getElementById('admin-file-input').click()}\n                                    disabled={sendingReply}\n                                    title=\"Attach document, image, or video\"\n                                    className=\"p-1.5 mr-2 text-gray-500 hover:text-white transition shrink-0\"\n                                  >\n                                    <svg xmlns=\"http://www.w3.org/2000/svg\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" strokeWidth=\"2\" strokeLinecap=\"round\" strokeLinejoin=\"round\" className=\"w-5 h-5\"><path d=\"M21.44 11.05-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48\"></path></svg>\n                                  </button>\n\n                                  <textarea\n                                    ref={replyInputRef}\n                                    rows={1}\n                                    placeholder=\"Type feedback, suggestions or replies...\"\n       
<truncated 2868 bytes>
REPLACEMENT:
"                              <form onSubmit={handleSendReply} className=\"p-4 border-t border-white/5 bg-zinc-950/10 flex gap-3 items-center\">\n                                <div className=\"flex-grow flex items-center bg-white/[0.03] border border-white/10 rounded-2xl px-3 py-1.5 focus-within:border-brand-cyan/40 transition duration-300\">\n                                  {isAdminRecording ? (\n                                    <div className=\"flex-grow flex items-center justify-between py-1\">\n                                      <div className=\"flex items-center gap-2 text-rose-500 font-mono text-xs font-bold uppercase\">\n                                        <span className=\"w-2.5 h-2.5 rounded-full bg-rose-500 animate-pulse\"></span>\n                                        <span>Recording {Math.floor(adminRecordingDuration / 60)}:{(adminRecordingDuration % 60).toString().padStart(2, '0')}</span>\n                                      </div>\n                                      <button \n                                        type=\"button\" \n                                        onClick={cancelAdminRecording}\n                                        className=\"text-rose-500 hover:text-rose-400 font-mono text-xs font-bold uppercase transition\"\n                                      >\n                                        Cancel\n                                      </button>\n                                    </div>\n                                  ) : (\n                                    <>\n                                      {/* Hidden File Input */}\n                                      <input \n                                        type=\"file\" \n                                        id=\"admin-file-input\" \n                                        className=\"hidden\" \n                                        onChange={handleAdminFileUpload} \n                                        disabled={sendingReply}\n                                      />\n           
<truncated 5000 bytes>

==================================================
STEP: 211
DESC: "Add inline base64 data URL fallback for file uploads and voice notes in web admin panel when the Apps Script fails or lacks Drive permissions."
==================================================
TARGET:
"      const handleAdminFileUpload = async (e) => {\n        const file = e.target.files[0];\n        if (!file || !selectedChatId) return;\n\n        setSendingReply(true);\n\n        try {\n          const reader = new FileReader();\n          reader.onload = async () => {\n            try {\n              const base64Data = reader.result.split(',')[1];\n              \n              let type = 'document';\n              if (file.type.startsWith('image/')) type = 'image';\n              else if (file.type.startsWith('video/')) type = 'video';\n              else if (file.type.startsWith('audio/')) type = 'audio';\n\n              const payload = {\n                action: 'upload_file',\n                fileName: file.name,\n                mimeType: file.type,\n                base64Data: base64Data\n              };\n\n              const response = await fetch(GOOGLE_SHEET_WEBHOOK_URL, {\n                method: 'POST',\n                headers: { 'Content-Type': 'text/plain;charset=utf-8' },\n                body: JSON.stringify(payload),\n              });\n              const result = await response.json();\n\n              if (result && result.status === 'success' && result.fileUrl) {\n                const msgData = {\n                  sender: 'mentor',\n                  name: 'Anurag KM (Mentor)',\n                  fileUrl: result.fileUrl,\n                  fileName: file.name,\n                  type: type,\n                  timestamp: isFirebaseMock ? new Date() : firebase.firestore.FieldValue.serverTimestamp()\n                };\n\n                if (isFirebaseMock) {\n                  const updatedMessages = { ...mockMessages };\n                  if (!updatedMessages[selectedChatId]) updatedMessages[selectedChatId] = [];\n                  updatedMessages[selectedChatId].push({ id: `mentor_${Date.now()}`, ...msgData });\n                  setMockMessages(updatedMessages);\n                  setChatMessages(updatedMessages[selectedChatId]);\n                } else {\n                  await
<truncated 6571 bytes>
REPLACEMENT:
"      const handleAdminFileUpload = async (e) => {\n        const file = e.target.files[0];\n        if (!file || !selectedChatId) return;\n\n        setSendingReply(true);\n\n        try {\n          const reader = new FileReader();\n          reader.onload = async () => {\n            try {\n              const base64Data = reader.result.split(',')[1];\n              \n              let type = 'document';\n              if (file.type.startsWith('image/')) type = 'image';\n              else if (file.type.startsWith('video/')) type = 'video';\n              else if (file.type.startsWith('audio/')) type = 'audio';\n\n              const payload = {\n                action: 'upload_file',\n                fileName: file.name,\n                mimeType: file.type,\n                base64Data: base64Data\n              };\n\n              let fileUrl = '';\n              try {\n                const response = await fetch(GOOGLE_SHEET_WEBHOOK_URL, {\n                  method: 'POST',\n                  headers: { 'Content-Type': 'text/plain;charset=utf-8' },\n                  body: JSON.stringify(payload),\n                });\n                const result = await response.json();\n                if (result && result.status === 'success' && result.fileUrl) {\n                  fileUrl = result.fileUrl;\n                } else {\n                  throw new Error(result.message || 'Server upload failed');\n                }\n              } catch (uploadErr) {\n                console.warn(\"Google Drive upload failed, falling back to base64 inline:\", uploadErr);\n                if (file.size > 800 * 1024) {\n                  throw new Error(\"File is too large (>800KB) for database inline storage fallback. Please authorize Apps Script Drive permissions: \" + uploadErr.message);\n                }\n                fileUrl = reader.result;\n              }\n\n              const msgData = {\n                sender: 'mentor',\n                name: 'Anurag KM (Mentor)',\n                fileUrl: fileUrl,\n       
<truncated 7365 bytes>
